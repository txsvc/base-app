<% content_for :page_title, formatted_title('Resend Instructions') %>

<h2 class="ui image header">
    <div class="content">Resend unlock instructions</div>
</h2>

<%= render "accounts/shared/error_messages", resource: resource %>

<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { class: 'ui form', method: :post }) do |f| %>

<div class='ui segment'>

    <div class='field'>
        <div class='ui left icon input'>
            <i class='user icon'></i>
            <%= f.email_field :email, autofocus: true, autocomplete: 'email', placeholder: 'E-mail address' %>
        </div>
    </div>

    <div class="actions">
        <%= f.submit 'Resend unlock instructions', class: 'ui fluid large submit button' %>
    </div>

</div>

<% end %>

<%= render "accounts/shared/links" %>