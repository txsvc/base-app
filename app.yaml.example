runtime: ruby27

instance_class: F1

env_variables:
  RAILS_ENV: "production"
  RAILS_MASTER_KEY: "ab3..."
  BASE_URL: "your_public_url"
  # Google Cloud Platform
  GCP_DB_USER: "root"
  GCP_DB_PASSWORD: "your_password"
  GCP_DB_SOCKET: "/cloudsql/project_id:regions:db_instance_id"
  
beta_settings:
  cloud_sql_instances: "project_id:regions:db_instance_id"
